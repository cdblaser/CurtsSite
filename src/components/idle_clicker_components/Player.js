// import React, { useState } from "react";

// const Player = (props) => {
//   const [player, setPlayer] = useState(props.fillRect(200, 400, 40, 40));
//   const [x, setX] = useState(400);
//   const playerColor = (props.fillStyle = "rgb(200, 200, 0)");
//   const intervalId = setInterval(() => {
//     setX(x + 1);
//     setPlayer(props.fillRect(200, x, 40, 40));
//   }, 500);
// };

// export default Player;
